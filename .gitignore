# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# env — never commit secrets (.env.example is the only committed env file)
.env
.env.*
!.env.example

# Personal/sensitive AI working notes (CLAUDE.md is the shared, committed one)
CLAUDE.local.md

# Internal documentation (kept out of the repo for now; easy to add back later).
# Add internal-only docs here. Version spec files are not committed.
encore_v*_spec.md

# Vercel
.vercel/

# Convex local artifacts (convex/_generated/ IS committed — needed for typecheck/build)
.convex/

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android
